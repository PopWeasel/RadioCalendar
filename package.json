{
  "name": "RadioCalendar",
  "version": "1.0.0",
  "license": "ISC",
  "scripts": {
    "start": "concurrently --kill-others \"cd server && CONFIG=../config/app-config.json npm start\" \"cd client && npm start\"",
    "dev": "concurrently --kill-others \"cd server && CONFIG=../config/app-config.json npm run dev\" \"cd client && npm run dev\""
  },
  "dependencies": {
    "@material-ui/icons": "^1.0.0",
    "concurrently": "^3.5.1",
    "npm": "^6.1.0"
  },
  "devDependencies": {
    "nodemon": "^1.17.4",
    "url-loader": "^1.0.1"
  }
}
