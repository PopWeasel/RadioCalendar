{
  "name": "RadioCalendar",
  "version": "1.0.0",
  "license": "ISC",
  "scripts": {
    "start": "concurrently --kill-others \"cd server && CONFIG=../config/app-config.json npm start\" \"cd client && npm start\""
  },
  "dependencies": {
    "concurrently": "^3.5.1"
  }
}
